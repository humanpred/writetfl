% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tfl_table.R
\name{tfl_colspec}
\alias{tfl_colspec}
\title{Specify display properties for a single table column}
\usage{
tfl_colspec(
  col,
  label = NULL,
  width = NULL,
  align = NULL,
  wrap = FALSE,
  gp = NULL
)
}
\arguments{
\item{col}{Character scalar. Column name in the data frame passed to
\code{\link[=tfl_table]{tfl_table()}}.}

\item{label}{Character scalar or \code{NULL}. Display label for the column header.
\code{NULL} uses the column name. Use \code{"\\n"} for multiline headers.}

\item{width}{A \code{unit} object (e.g. \code{unit(1.5, "inches")}) or a plain
positive numeric (treated as a relative weight; columns with relative
weights are scaled to fill available width after fixed-width columns are
placed). \code{NULL} triggers content-based auto-sizing.}

\item{align}{Character scalar: \code{"left"}, \code{"right"}, or \code{"centre"}. \code{NULL}
defaults to \code{"right"} for numeric columns and \code{"left"} otherwise.}

\item{wrap}{Logical. Whether this column is eligible for word-wrapping when
total column widths exceed available width.}

\item{gp}{A \code{gpar()} object to override \code{\link[=tfl_table]{tfl_table()}}'s \code{gp$group_col} for
this specific column. Only valid for row-header (group) columns; an error
is raised if applied to a data column.}
}
\value{
An object of class \code{"tfl_colspec"}.
}
\description{
Creates a column specification object for use with \code{\link[=tfl_table]{tfl_table()}}. All
arguments are optional — unspecified properties fall back to the
corresponding flat argument in \code{tfl_table()}, or to type-based defaults.

You can use \code{tfl_colspec()} for fine-grained per-column control, or use the
flat arguments of \code{tfl_table()} (\code{col_widths}, \code{col_labels}, \code{col_align},
\code{wrap_cols}) for simpler cases. When both are provided for the same column,
the \code{tfl_colspec()} entry takes priority.
}
\seealso{
\code{\link[=tfl_table]{tfl_table()}}
}
