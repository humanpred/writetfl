% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_fig_as_pdf.R
\name{export_fig_as_pdf}
\alias{export_fig_as_pdf}
\title{Export a list of figures to a multi-page PDF}
\usage{
export_fig_as_pdf(
  x,
  file,
  pg_width = 11,
  pg_height = 8.5,
  page_num = "Page {i} of {n}",
  ...
)
}
\arguments{
\item{x}{A single \code{ggplot} object, or a named list of page specifications.
Each page specification is a list with a required \code{figure} element
(a \code{ggplot} object) and optional elements corresponding to the text
arguments of \code{\link[=export_figpage_to_pdf]{export_figpage_to_pdf()}}: \code{header_left}, \code{header_center},
\code{header_right}, \code{caption}, \code{footnote}, \code{footer_left}, \code{footer_center},
\code{footer_right}. Per-page list elements take precedence over values
supplied via \code{...}.}

\item{file}{Path to the output PDF file. Must be a single character string
ending in \code{".pdf"}.}

\item{pg_width}{Page width in inches.}

\item{pg_height}{Page height in inches.}

\item{page_num}{A \code{\link[glue:glue]{glue::glue()}} specification for automatic page numbering,
where \code{{i}} is the current page number and \code{{n}} is the total number of
pages. Set to \code{NULL} to disable.}

\item{...}{Additional arguments passed to \code{\link[=export_figpage_to_pdf]{export_figpage_to_pdf()}}.
These serve as defaults for all pages and are overridden by per-page
list elements in \code{x}.}
}
\value{
The normalized absolute path to the PDF file, returned invisibly.
}
\description{
Opens a PDF device, renders each page using \code{\link[=export_figpage_to_pdf]{export_figpage_to_pdf()}}, and
closes the device. Guarantees device closure via \code{on.exit()} even if an
error occurs during rendering.
}
\examples{
\dontrun{
library(ggplot2)

# Single plot
p <- ggplot(mtcars, aes(wt, mpg)) + geom_point()
export_fig_as_pdf(p, "single.pdf")

# Multiple plots with per-page captions
plots <- list(
  list(figure = p, caption = "Weight vs MPG"),
  list(figure = ggplot(mtcars, aes(hp, mpg)) + geom_point(),
       caption = "Horsepower vs MPG")
)
export_fig_as_pdf(plots, "report.pdf",
  header_left  = "My Report",
  header_right = format(Sys.Date())
)
}

}
\seealso{
\code{\link[=export_figpage_to_pdf]{export_figpage_to_pdf()}} for single-page layout control.
}
