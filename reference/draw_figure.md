# Draw a figure (ggplot or future types) inside a viewport

Draw a figure (ggplot or future types) inside a viewport

## Usage

``` r
draw_figure(fig, vp)
```

## Arguments

- fig:

  A ggplot object. Future: grob, recordedPlot.

- vp:

  A viewport object.
