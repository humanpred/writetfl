# Wrap a column-header label (or any single string) to a target width minus left+right horizontal padding.

Wrap a column-header label (or any single string) to a target width
minus left+right horizontal padding.

## Usage

``` r
.wrap_label_for_width(label, width_in, h_pad_in, gp, breaks)
```
