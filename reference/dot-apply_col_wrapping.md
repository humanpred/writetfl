# Iteratively narrow wrap-eligible columns until total fits or all at min

Iteratively narrow wrap-eligible columns until total fits or all at min

## Usage

``` r
.apply_col_wrapping(
  widths_in,
  resolved_cols,
  data,
  tbl,
  content_width_in,
  min_in,
  h_pad_in,
  na_str,
  max_rows,
  pg_width,
  pg_height,
  margins
)
```
