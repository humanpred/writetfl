# Normalize text input to a single string with line count

Normalize text input to a single string with line count

## Usage

``` r
normalize_text(x)
```

## Arguments

- x:

  NULL, a single character string, or a character vector.

## Value

A list with elements `text` (single string or NULL) and `nlines`
(integer).
