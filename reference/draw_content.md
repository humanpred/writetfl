# Draw the page content (ggplot or grob) inside a viewport

Draw the page content (ggplot or grob) inside a viewport

## Usage

``` r
draw_content(content, vp)
```

## Arguments

- content:

  A ggplot object or any grid grob (including gtable).

- vp:

  A viewport object.
